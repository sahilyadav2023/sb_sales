// server component for the /works route
export const metadata = {
  title: "Past Works — SteelCraft Fabricators",
};

export default function WorksLayout({ children }: { children: React.ReactNode }) {
  return children;
}